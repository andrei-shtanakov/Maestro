"""Pinned copy of steward's gate catalog and its normative contract text.

Vendored, never referenced: `../steward/` exists only in this development
workspace, so shipped code resolves the catalog from inside the package.
Files under `upstream/` mirror their upstream paths verbatim — see
`maestro.gate_catalog` for the pin, the digests and the namespace rule.
"""
